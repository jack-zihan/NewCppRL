SAC 网络架构快速参考
====================

输入维度：
- observation: [Batch, Channels, 128, 128]  # 视觉输入
- vector: [Batch, 1]                        # 状态向量
- action: [Batch, 2]                        # 动作（仅Q网络）

=============================================================
POLICY NETWORK (Actor) - 生成动作分布
=============================================================
                    Input: observation [B, C, 128, 128]
                            +
                          vector [B, 1]
                            ↓
┌──────────────────────────────────────────────────────────┐
│  CNN Encoder:                                            │
│  Conv1(C→32) + MaxPool → [B, 32, 64, 64] → ResNet×2     │
│  Conv2(32→64) + MaxPool → [B, 64, 32, 32] → ResNet×2    │
│  Conv3(64→64) + MaxPool → [B, 64, 16, 16] → ResNet×2    │
│  Flatten → [B, 16384]                                    │
└──────────────────────────────────────────────────────────┘
                            ↓
                  Concat with vector → [B, 16385]
                            ↓
                    Linear(16385 → 512) + SiLU
                            ↓
                    Linear(512 → 512) + SiLU
                            ↓
                    Linear(512 → 4)
                            ↓
                 NormalParamExtractor
                            ↓
              Output: loc [B, 2], scale [B, 2]
                     (动作均值和标准差)

=============================================================
Q-VALUE NETWORK (Critic) - 评估状态-动作对
=============================================================
                    Input: observation [B, C, 128, 128]
                            +
                          vector [B, 1]
                            +
                          action [B, 2]
                            ↓
┌──────────────────────────────────────────────────────────┐
│  CNN Encoder: (与Policy Network相同结构)                 │
│  Conv1→Conv2→Conv3 → Flatten → [B, 16384]               │
└──────────────────────────────────────────────────────────┘
                            ↓
            Concat with vector & action → [B, 16387]
                            ↓
                    Linear(16387 → 512) + SiLU
                            ↓
                    Linear(512 → 512) + SiLU
                            ↓
                    Linear(512 → 1)
                            ↓
                    Output: Q-value [B, 1]

=============================================================
关键参数总结
=============================================================
总参数量估算：
- Policy Network: ~8.5M parameters
- Q-Value Network: ~8.5M parameters
- 总计: ~17M parameters

激活函数: SiLU (Swish)
动作分布: TanhNormal (有界连续动作)
优化特性: ResNet连接, 参数共享设计